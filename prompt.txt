Login:
Actúa como un desarrollador Senior Frontend. 
Crea una página de inicio de sesión (Login) profesional y moderna. 

Especificaciones técnicas y de diseño:
- Tecnologías: HTML5, CSS3 (puedes usar Tailwind CSS para el estilo) y JavaScript vanilla.
- Interfaz: Un contenedor elegante y centrado vertical y horizontalmente, con sombras suaves y bordes redondeados.
- Campos: Input para 'Correo Electrónico' (con validación de tipo email) e Input para 'Contraseña' (con enmascaramiento de caracteres).
- Funcionalidad: Validación básica de campos obligatorios mediante JavaScript antes de simular el envío.
- Estética: Diseño minimalista, paleta de colores profesional (White y Orange), tipografía moderna y totalmente responsivo para dispositivos móviles.
- Restricción: No incluyas ninguna sección, enlace o botón para 'Registrarse' o 'Crear cuenta'.

Animaciones: 
- Al cargar la página, el contenedor debe aparecer con una animación de fade-in y slide-up.
- Los campos de entrada deben tener un efecto de focus con un borde animado.
- El botón de inicio de sesión debe tener un efecto de hover con un cambio de color y un ligero aumento de tamaño.
- Al enviar el formulario, debe aparecer un mensaje de error o éxito con una animación de fade-in y slide-up.

Estructura de carpeta:
- CSS
- HTML
- JS
- IMG

Limitaciones:
- No incluyas ninguna sección, enlace o botón para 'Registrarse' o 'Crear cuenta'.
- No hay opcion de "Olvide la contraseña" (la creación y asignación de usuarios y contraseña será manual)

Cambios:
- Errores contextuales, especificar que parte ha dado error en especifico 
- Redireccionamiento directo y almacenamiento temporal de datos en local (nombre de usuario, tipo de usuario y email de usuario)

Extractor de facturas:
Actúa como desarrollador Frontend Senior.
Realiza la conexión de redireccionamiento entre el Login (al ser validado) a la nueva página "extractor-de-facturas.html", con un timeout de 1500 ms.
Mostrando que ha sido verificado como valido el usuario y contraseña.
Con una estructura como:
    window.location.href = 'extractor-de-facturas.html'; // Ajusta la ruta relativa según donde coloques el archivo

Cambios esteticos: (Img Figma Referencia)
- IMPORTANTE: Elementos de lógica (subir archivos, rellenar campos, botones, revisar, ver imágen) mantendrán su lógica original
- El resto de elementos deben seguir el diseño de la imagen de referencia.
- Mantener la estructura de la página, pero con el nuevo diseño.
- Colores: Usar colores del Figma (azul), pero con el nuevo diseño, el color es temporal.
- Tipografía: Usar la tipografía del Figma (Inter).
- Animaciones: Hacer que la lista de "Productos Pendientes por Revisión" sea una lista scrolleable.

Notas de placeholders:
- Barra de busqueda: sin lógica, solo placeholder
    En la Sidebar:
- Inforamción personal: en el recuadro superior izquierdo se deberá ver la información personal (nombre, tipo y email) del usuario.
- Crear Boton "Subir Facturas": sin lógica, solo placeholder
- Crear Boton "Subir guías": sin lógica, solo placeholder
- Crear Boton "Ver Productos": sin lógica, solo placeholder
- Crear Boton "Tracking ML": sin lógica, solo placeholder

Modificaciones esteticas:
    (1) Paginación:
- Estado inicial: traerá los 20 primeros items de la base de datos.
- Boton: "Mostrar más", desplegará 20 productos más de la lista, solo activo mientras existan productos por mostrar.
- Boton: "Mostrar menos", ocultará los últimos 20 productos de la lista, solo activo mientras existan más de 20 productos mostrados.
- Boton: "Mostrar todos", mostrará todos los productos de la lista.

    (2) Logotipo:
- El logotipo actual (superior derecha) es un placeholder, se debe reemplazar por el "Logo_empresa.png" (placeholder montaña).
- El logotipo actual (sidebar, superior izquierda) es un placeholder, se debe reemplazar por el "Logo_equipo.png" (placeholder ojo).

    (3) responsiveness:
- Sidebar: la sidebar será un desplegable y responsiva, mediante las Tecnologías usadas (HTML5, CSS, JS)
    - En pantallas pequeñas (móviles), la sidebar se ocultará y aparecerá un botón de menú para desplegarla.
    - En pantallas medianas (tablets), la sidebar se mostrará parcialmente y aparecerá un botón de menú para desplegarla completamente.
    - En pantallas grandes (desktops), la sidebar se mostrará completamente y aparecerá un botón de menú para ocultarla.
    - HTML: Usa etiquetas semánticas (<nav>, <aside>, <ul>). Incluye un botón de "toggle" con un "hamburger" icono (puedes usar un placeholder para el icono).
    - CSS: Añade transiciones suaves (transition) para el cambio de ancho, la sidebar debe ser "collapsible" (ancho total vs. ancho de solo iconos).
    - Asegura que sea totalmente responsiva (en móviles debería ocultarse por completo o volverse un menú inferior).
    - JS: Añade la lógica para el "toggle" de la sidebar, con la estructura:
        - Usa classList.toggle para manejar el estado de apertura/cierre.
        Asegura que el estado se mantenga mediante una clase en el body o el contenedor principal.
    Correcciones:
    - El toggle de la sidebar debe acompañar a la sidebar cuando se despliega, no debe quedarse en la misma posicion
    - En pantllas grandes (desktops), al dar click en el toggle, la sidebar que esta visible en predeterminado debe de ocultarse con una transicion suave y el contenido de la pagina debe de ajustarse para ocupar el espacio de la sidebar, de igual manera al dar click en el toggle, la sidebar debe de aparecer con una transicion suave y el contenido de la pagina debe de ajustarse para ocupar el espacio de la sidebar.    

17/02/2026
Correciones nuevas: Puramente esteticas
- Cambio de idioma WELCOME a BIENVENIDO
- Quitar parrafo "Sube hasta 80 archivos y envíalos uno por uno cada 5 segundos"
- Reemplazar columna ID por una columna llamada "Fecha", cuyos datos serán de la columna "created_at" de la base de datos de Supabase
- Cambiar colores de la tabla: primera fila será rgba(253, 90, 3) y las demás filas serán tono gris claro.
- Cambiar colores en la sidebar: el color de fondo de la tarjeta de usuario será rgba(253, 90, 3) y el color general de la dashboard se mantendrá igual.
- Cambiar de color la sección de "Productos Pendientes por Revisión", y añadir una sección al lado de "Productos Revisados", la sección en selección (por predeterminado "Productos Pendientes por Revisión") será de color de fondo rgba(253, 90, 3) y el color de la sección no seleccionada será de color de texto rgba(253, 90, 3) atenuado. Adapta el tamaño de la searchbar (reducir) para que entre bien este nueva sección de "Productos Revisados" (div)
- Quitar tanto el logotipo "Logo_empresa.png" como el logotipo "Logo_equipo.png", donde estaba el "Logo_equipo.png" ahora colocar "Logo_empresa_noFondo.png".
- Que el color de nombre de Usuario (después de BIENVENIDO) sea de color rgba(253, 90, 3).
- En la sección de "Productos Revisados" cambia la columna de Estado por una columna llamada "Validado", y en lugar de que sea un check, tiene que ser un checkbox como el de la sección "Productos Pendientes por Revisión" para que el usuario pueda marcar y desmarcar el producto como validado.
- Aumenta levemente (1 o 2 puntos) el tamaño de la imagen "Logo_empresa_noFondo.png".
- Al final despues de la columna Revisar debe haber una columna nueva sin nombre, tan solo un icono de "papelera" que al darle click, se eliminará el producto de la lista.

Correciones parte 2: Esteticas y organizativas
-  Se debe cambiar la presentación estetica de la sección de "Subir archivos" (anexo imagen), en lugar de que se deba hacer clic en la sección "Elegir", todo el recuadro debe ser el clickeable (todo el recuadro grande animado será el "Elegir").
- El boton "Enviar archivos" se mantendrá igual, solo que el color de fondo cambiará a rgba(253, 90, 3) y el color del texto seguirá siendo blanco. 
- El texto "Subir archivos" estará centrado y será más grande (en el centro del recuadro clickeable), el texto de "Sin archivos seleccionados" | "X archivos seleccionados" estará debajo del texto "Subir archivos" y será más pequeño (en el centro del recuadro clickeable).
- Los componentes "Productos Pendientes por Revisión", "Productos Revisados", filtrado, searchbar, Exportar estarán uno encima de otro en la vista de móviles, y en desktop y tablet estarán uno al lado del otro (como predeterminado).  
- En la parte inferior izquierda de la pantalla, cambiar la versión "v1.20" por el nombre de la empresa "Alphat Service" con un pequeño logotipo usando la imagen "Logo_equipo.png" a la izquierda del texto.
- Al tener el campo de Fecha (created_at) en la tabla, ahora seria oportuno agregar la opción de filtrado por fecha, que consiste en elegir 2 fechas (desplegar un calendario) y que la tabla muestre los productos que esten en ese rango de fechas. 
    - A nivel de colocación este filtro por fechas estará al lado izquierdo del searchbar (en desktop y tablet), y encima de la searchbar.

- En la sidebar (esquina inferior izquierda) donde esta el toggle de modo oscuro-claro, el toggle debe de ser accionable, es decir, que al darle click se cambie de modo oscuro-claro.
    Definición de modo claro: el predeterminado que esta diseñado.
    Definición de modo oscuro: colores naranjas se mantienen, fondo se vuelve oscuro (no necesariamente negro) y letras en blanco, botones y secciones de colores (naranja, verde) no se ven afectadas. El sidebar se mantendrá igual.  

- Correción de filtrado de fecha: al desplegar el filtrado de fecha, la Start Date debe ser  máximo el dia de hoy (en el calendario), la End Date debe ser minimo el día seleccionado en la Start Date (en el calendario). Si se selecciona una fecha en la Start Date, la End Date debe de actualizarse automaticamente para que sea minimo el día seleccionado en la Start Date. En caso de que no se haya seleccionado una fecha en el Start Date, la End Date no tendrá limitaciones de selección.

--- Cambio Relevante ---
Refactorización: 
1. Separar del archivo "extractor-de-facturas.html" el codigo script, llevar dicho script a un codigo JavaScript llamado extractor.js (en la carpeta JS), y en el archivo HTML llamar a dicho script con la etiqueta <script src="extractor.js"></script>.
2. Separar del archivo "extractor-de-facturas.html" las secciones de style (CSS), llevar dicho CSS a un archivo CSS llamado extractor.css (en la carpeta CSS), y en el archivo HTML llamar a dicho CSS con la etiqueta <script src="https://cdn.tailwindcss.com"></script> <link rel="stylesheet" href="CSS/extractor.css">.
3. Limpiar el HTML de "extractor-de-facturas.html" de todo el codigo script y CSS, dejando solo el HTML y las etiquetas <script src="extractor.js"></script>, <script src="https://cdn.tailwindcss.com"></script> y <link rel="stylesheet" href="CSS/extractor.css">. Verificar que el cambio se existoso y que la página funcione con normalidad (exactamente igual) tras la Refactorización.

Verificar que los datos de la tabla inicial "Productos Pendientes por Revisión" sean cargados exitosamente, que no se quede en un bucle infinito de "Cargando productos..."

- Implementación de la barra de búsqueda global (searchGlobal) HTML:
1. Agrégale el atributo id="searchInput". (Listo manualmente)
2. Agrégale el atributo id="filterSelect" (opcional, si planeas poner un selector de filtro SKU/ID/Titulo al lado, si no, omite este punto).
3. Asegúrate de no romper las clases de Tailwind existentes.
- Lógica y comunicación de función de busqueda SQL en extractor.js: Implementar la lógica de búsqueda por SQL (RPC) e integrarla con el sistema actual.
1. Variables Globales: Al inicio, junto a let currentTab, agrega:
let currentSearchTerm = '';
let searchDebounceTimer;
2. Modificar loadProducts: Busca la función loadProducts. Agrega esta línea al principio de la función para evitar que el auto-refresh borre una búsqueda activa:
// Si hay una búsqueda activa, no recargar la lista por defecto
if (currentSearchTerm.length > 0) return;
3. Insertar Nueva Lógica: Agrega las siguientes funciones antes de renderProducts():
// --- LÓGICA DE BÚSQUEDA (RPC SQL) ---
// 1. Manejador del Input (Debounce)
window.handleSearchInput = function(e) {
    const query = e.target.value.trim();
    currentSearchTerm = query;
    clearTimeout(searchDebounceTimer);
    if (query.length === 0) {
        // Si borra el texto, recargamos la vista normal
        loadProducts();
        return;
    }
    // Esperar 300ms antes de llamar a la API
    searchDebounceTimer = setTimeout(() => {
        performSearch(query);
    }, 300);
};
// 2. Ejecutar la búsqueda en Supabase
async function performSearch(term) {
    const container = document.getElementById('productsTableContainer');
    // Mostrar Loading
    container.innerHTML = `
       <div class="flex items-center justify-center h-full text-gray-500 italic">
        <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-primary mr-3"></div>
        Buscando "${term}"...
       </div>`;
    try {
        // Llamada a la función SQL 'search_products_pro'
        const { data, error } = await supabaseClient
            .rpc('search_products_pro', {
                search_term: term,
                search_mode: 'all', // Opcional: podrías conectar esto a un select
                page_number: 1,
                page_size: 50
            });
        if (error) throw error;
        // Actualizamos los datos globales y renderizamos
        currentProducts = data;
        visibleCount = 50; // Mostrar más resultados en búsqueda
        renderProducts();
    } catch (err) {
        console.error("Error en búsqueda:", err);
        container.innerHTML = `<div class="text-red-500 text-center p-4">Error buscando: ${err.message}</div>`;
    }
}
4. Conectar evento: Dentro de startApp(), busca donde se inicializan los listeners o al final de la función, y agrega:
const searchInput = document.getElementById('searchInput');
if (searchInput) {
    searchInput.addEventListener('input', window.handleSearchInput);
} else {
    console.warn("Input de búsqueda no encontrado (ID: searchInput)");
}

Separación lógica de la inicialización de Supabase (dia 2, 18-02-2026):
- Crear archivo supabase-client.js en la carpeta JS/shared (ya listo manualmente)
- Pega el siguiente código JavaScript que inicializa el cliente de Supabase y lo expone globalmente en window.sbClient
- Manten la lógica de extracto.js para que funcione correctamente

- También hacer ese cambio en script.js (por consecuencia en index.html), pero claro manteniendo la lógica de extracto.js para que funcione correctamente.
// js/shared/supabase-client.js

const SUPABASE_URL = 'https://voedeobojnlsjfxhkbhy.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZvZWRlb2Jvam5sc2pmeGhrYmh5Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTU5ODM3MzcsImV4cCI6MjA3MTU1OTczN30.ySOeKnQLKomzyh3tFSaNJdiHD5amS2fmSpKFzFZJNLA';

// Verificamos si la librería de Supabase se cargó desde el CDN
if (typeof window.supabase !== 'undefined') {
    // Inicializamos y lo guardamos en una variable global 'sbClient'
    window.sbClient = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
    console.log("✅ Supabase inicializado globalmente.");
} else {
    console.error("❌ La librería de Supabase no está cargada. Asegúrate de incluir el script del CDN en el HTML antes de este archivo.");
}

Filtrado por SKU, ID y Título (dia 3, 19-02-2026):
- Implementar la lógica de filtrado por SKU, ID y Título en extractor.js
- Agregar un select en el HTML para seleccionar el tipo de filtro, justo al lado del input de búsqueda global.  
